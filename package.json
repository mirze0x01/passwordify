{
  "name": "passwordify",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first password generator and encrypted Chrome extension vault.",
  "scripts": {
    "check": "node scripts/check-extension.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
